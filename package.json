{
  "name": "xyz.rsmq.single.bootstrap",
  "version": "0.3.0",
  "description": "simple message queue bootstrap for xyz",
  "main": "xyz.rsmq.js",
  "scripts": {
    "test": "./node_modules/.bin/mocha --reporter spec",
    "test-travis": "rm -Rf node_modules && npm install && ./node_modules/.bin/mocha --reporter spec"
  },
  "author": "kian.peymani@gmail.com",
  "license": "ISC",
  "dependencies": {
    "rsmq-worker": "^0.5.2"
  },
  "devDependencies": {
    "chai": "^3.5.0",
    "mocha": "^2.5.3",
    "xyz-cli": "latest",
    "xyz-core": "latest"
  }
}
